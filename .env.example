# Copy to owui.env (on the real machine) and fill in the values.
# DO NOT commit the real .env file: it contains the admin credentials.
WEBUI_ADMIN_EMAIL=admin@example.com
WEBUI_ADMIN_PASSWORD=change-me
WEBUI_ADMIN_NAME=admin
OPENAI_API_BASE_URLS=http://localhost:8080/v1
